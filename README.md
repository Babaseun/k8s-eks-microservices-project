# Simple mircoservices project

This project involves four different services. Communication between these services is handled through an API gateway using Nginx to proxy the requests, both within and outside of a Kubernetes cluster. Additionally, CI/CD pipelines are implemented using GitHub Actions to automate the build, test, and deployment processes.
